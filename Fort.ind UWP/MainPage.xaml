<Page
    x:Class="Fort.ind_UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fort.ind_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <!-- PURPLE SIDEBAR -->
        <Grid Width="220" Background="#6A0DAD" HorizontalAlignment="Left">
            <StackPanel Margin="10" VerticalAlignment="Stretch">

                <TextBlock Text="Menu"
                           FontSize="22"
                           Foreground="White"
                           Margin="0,0,0,20"/>

                <Button Content="Latest News" Click="LatestNews_Click" Margin="0,5"/>
                <Button Content="Games" Margin="0,5"/>
                <Button Content="Betas" Margin="0,5"/>
                <Button Content="Contacts" Margin="0,5"/>
                <Button Content="Social" Margin="0,5"/>
                <Button Content="About" Margin="0,5"/>
            </StackPanel>
        </Grid>

        <!-- MAIN CONTENT AREA -->
        <Grid Margin="220,0,0,0">

            <!-- Latest News Content -->
            <ScrollViewer x:Name="LatestNewsPanel" Visibility="Collapsed">
                <StackPanel Padding="20">
                    <TextBlock Text="Fort.ind App – Beta Release"
                               FontSize="32"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="Our app is now officially in beta! 🎉"
                               FontSize="18"
                               Margin="0,0,0,10"/>

                    <TextBlock Text="This early version includes basic features such as quick navigation, preview pages, and simple interface layouts. 
We are actively working on stability improvements and new design enhancements. Thank you for testing the beta version of Fort.ind!"
                               TextWrapping="Wrap" 
                               FontSize="16"/>

                </StackPanel>
            </ScrollViewer>

        </Grid>

    </Grid>
</Page>
